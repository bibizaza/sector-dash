# Sector dashboard

Static publish target for `etf_analyzer/tools/sector_dash.py`. `index.html` is
generated — edit the generator, not this repo. Rebuilt and pushed each morning
by `ch.herculis.sectordash`.
